
dataset: {
    'dataset_name': 'm4',
    'frequency': 'daily',
    'min_series_length': 730,
    'max_series_length': 1100, 
    'samples': 10,
    'ext': '.parquet',
    'seed': 1992
}

fitting: {
    'retrain_scenarios': [
        7, 
        # 14, 
        # 21,
        # 30, 
        # 60,
        # 90,
        # 120,
        # 150,
        # 180,
        # 364
    ],
    levels: [50, 60, 70, 80, 90, 95, 99],
    combine_only: False
}

model_names: [
    'LinearRegression', 
    'LGBMRegressor', 
    'MLP',
    'NBEATSx'
]

evaluation: {
    metrics: ['bias', 'mae', 'mse', 'rmse', 'mase', 'msse', 'rmsse', 'mql', 'smql', 'scrps'], # 'ql', 'cal', 'cov', 'sql'
    evaluation_frequency: 'daily', # frequency for mase, msse, rmsse
    evaluation_sample_type: 'overlap' # nooverlap
}
