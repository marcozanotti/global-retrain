
dataset: {
    'dataset_name': 'm5',
    'frequency': 'monthly',
    'min_series_length': 42, 
    'samples': null,
    'ext': '.parquet',
    'seed': 1992
}

fitting: {
    'retrain_scenarios': [
        1, 
        2, 
        3,
        4, 
        5,
        6,
        9,
        12,
        15,
        18
    ],
    levels: [50, 60, 70, 80, 90, 95, 99]
}

model_names: [
    'LinearRegression', 
    'RandomForestRegressor', 
    'XGBRegressor', 
    'LGBMRegressor', 
    'CatBoostRegressor'
]

evaluation: {
    metrics: ['bias', 'mae', 'mse', 'rmse', 'mase', 'msse', 'rmsse', 'mql', 'cov', 'scrps'], # 'ql', 'cal'
    evaluation_sample_type: 'overlap' # nooverlap
}
