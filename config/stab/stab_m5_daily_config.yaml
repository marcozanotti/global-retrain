
dataset: {
    'dataset_name': 'm5',
    'frequency': 'daily',
    'ext': '.parquet'
}

fitting: {
    'retrain_scenarios': [
        7, 
        14, 
        21,
        30, 
        60,
        90,
        120,
        150,
        180,
        364
    ],
    levels: [50, 60, 70, 80, 90, 95, 99],
    combine_only: False
}

model_names: [
    # 'LinearRegression', 
    # # 'RandomForestRegressor', 
    # 'XGBRegressor', 
    # 'LGBMRegressor', 
    # 'CatBoostRegressor',
    # 'MLP',
    # # 'LSTM',
    # 'TCN',
    # 'NBEATSx',
    # 'NHITS',
    # 'EnsembleMean2A',
    # 'EnsembleMean2T',
    # 'EnsembleMean3A',
    # 'EnsembleMean3T',
    # 'EnsembleMean4A',
    # 'EnsembleMean4T',
    'EnsembleMean5A',
    'EnsembleMean5T'
]

evaluation: {
    metrics: ['stab_bias', 'mac', 'rmsc', 'smapc', 'mqlc'],
    skip: 7
}
