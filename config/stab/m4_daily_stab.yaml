dataset: {
    'dataset_name': 'm4',
    'frequency': 'daily',
    'min_series_length': 1100, # 2200 for 364 retrain scenario, 1100 for others sf, 1100 for ml and dl
    'max_series_length': 1100, # 2200 for 364 retrain scenario, 1100 for others sf, 1100 for ml and dl
    'samples': null, # 1000 for sf models, null for ml and dl models
    'ext': '.parquet',
    'seed': 1992
}

fitting: {
    'retrain_scenarios': [
        7, 
        14, 
        21,
        30, 
        60,
        90,
        120,
        150,
        180,
        364
    ],
    levels: [50, 60, 70, 80, 90, 95, 99],
    combine_only: False
}

model_names: [
    # 'ETS',
    # 'ARIMA',
    # 'LinearRegression', 
    # 'RandomForestRegressor',
    # 'XGBRegressor',
    # 'LGBMRegressor',
    # 'MLP',
    # 'TCN',
    # 'NBEATSx',
    'NHITS',
    # 'EnsembleMean2A',
    # 'EnsembleMean3A',
    # 'EnsembleMean4A',
    # 'EnsembleMean5A'
]

evaluation: {
    metrics: ['smapc', 'smqpc'],
    skip: 1,
    evaluation_frequency: 'daily', # frequency for masc, rmssc, smqc
}
