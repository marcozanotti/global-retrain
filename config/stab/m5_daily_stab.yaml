dataset: {
    'dataset_name': 'm5',
    'frequency': 'daily',
    'min_series_length': 1460, # 1800 for 364 retrain scenario, 1460 for others sf, 730 for ml and dl
    'max_series_length': null, 
    'samples': 1000, # 1000 for sf models, null for ml and dl models
    'ext': '.parquet',
    'seed': 1992
}

fitting: {
    'retrain_scenarios': [
        7, 
        14, 
        21,
        30, 
        60,
        90,
        120,
        150,
        180,
        364
    ],
    levels: [50, 60, 70, 80, 90, 95, 99],
    combine_only: False
}

model_names: [
    'ETS',
    # 'LinearRegression', 
    # 'XGBRegressor', 
    # 'LGBMRegressor', 
    # 'CatBoostRegressor',
    # 'MLP',
    # 'TCN',
    # 'NBEATSx',
    # 'NHITS',
    # 'EnsembleMean2A',
    # 'EnsembleMean2T',
    # 'EnsembleMean3A',
    # 'EnsembleMean3T',
    # 'EnsembleMean4A',
    # 'EnsembleMean4T',
    # 'EnsembleMean5A',
    # 'EnsembleMean5T'
]

evaluation: {
    metrics: ['stab_bias', 'mac', 'masc', 'rmsc', 'rmssc', 'smapc', 'mqc', 'smqc'],
    skip: 7,
    evaluation_frequency: 'daily', # frequency for masc, rmssc, smqc
}
