dataset: {
    'dataset_name': 'vn1',
    'frequency': 'weekly',
    'min_series_length': 157,
    'max_series_length': null, 
    'samples': 1000, # 1000 for sf models, null for ml and dl models
    'ext': '.parquet',
    'seed': 1992
}

fitting: {
    'retrain_scenarios': [
        1, 
        2, 
        3,
        4, 
        6,
        8,
        10,
        13,
        26,
        52
    ],
    levels: [50, 60, 70, 80, 90, 95, 99],
    combine_only: False
}

model_names: [
    'ETS',
    'ARIMA',
    # 'LinearRegression', 
    # 'RandomForestRegressor', 
    # 'XGBRegressor', 
    # 'LGBMRegressor', 
    # 'CatBoostRegressor',
    # 'MLP',
    # 'LSTM',
    # 'TCN',
    # 'NBEATSx',
    # 'NHITS',
    # 'EnsembleMean2A',
    # 'EnsembleMean2T',
    # 'EnsembleMean3A',
    # 'EnsembleMean3T',
    # 'EnsembleMean4A',
    # 'EnsembleMean4T',
    # 'EnsembleMean5A',
    # 'EnsembleMean5T'
]

evaluation: {
    metrics: ['stab_bias', 'mac', 'masc', 'rmsc', 'rmssc', 'smapc', 'mqc', 'smqc'],
    skip: 1,
    evaluation_frequency: 'yearly', # frequency for masc, rmssc, smqc
}
