dataset: {
    'dataset_name': 'hapag_region',
    'frequency': 'weekly',
    'min_series_length': null,
    'max_series_length': 728, # 14 years of weekly data
    'samples': null,
    'ext': '.parquet',
    'seed': 1992
}

fitting: {
    'retrain_scenarios': [
        1, 
        2, 
        3,
        4, 
        6,
        8,
        10,
        13,
        26,
        52, 
        104
    ],
    levels: [50, 60, 70, 80, 90, 95, 99],
    combine_only: False
}

model_names: [
    # 'Naive',
    'ETS',
    'ARIMA',
    # 'LinearRegression',  
    # 'XGBRegressor', 
    # 'LGBMRegressor',
    # 'MLP',
    # 'NBEATSx'
]

evaluation: {
    metrics: ['smapc', 'smqpc'],
    skip: 1,
    evaluation_frequency: 'yearly', # frequency for masc, rmssc
}
