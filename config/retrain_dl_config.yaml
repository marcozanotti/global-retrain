
dataset: {
    dataset_name: 'm5',
    frequency: 'daily',
    min_series_length: 730, # 365 * 2 = 730
    samples: 10,
    ext: '.parquet',
    seed: 1992
}

fitting: {
    horizon: 28,
    test_window: 56, # test_window = 28 * 13 = 364
    retrain_scenarios: [
        # 7, 
        # 14, 
        # 21,
        30, 
        # 60,
        # 90,
        # 120,
        # 150,
        # 180,
        # 364
    ],
    'intervals': {
        'h': 28,
        'n_windows': 4,
        'method': 'conformal_distribution'
    },
    levels: [50, 60, 70, 80, 90, 95, 99],
    store_in_sample_results: False
}

model_names: [
    # 'LinearRegression', 
    # 'Lasso', 
    # 'Ridge', 
    # 'RandomForestRegressor', 
    # 'XGBRegressor', 
    # 'LGBMRegressor', 
    # 'CatBoostRegressor',
    'MLP',
    'LSTM',
    'TCN',
    'NBEATSx',
    'NHITS'
]
# model_params: null
model_params: {
    'MLP': {
        'h': 28,
        'input_size': 7,
        'num_layers': 2,
        'hidden_size': 128,
        'max_steps': 10,
        'stat_exog_list': ['item_id', 'dept_id', 'cat_id', 'store_id', 'state_id'],
        'futr_exog_list': ['year', 'quarter', 'month', 'week', 'dayofweek', 'day', is_weekend, 'event'],
        'random_seed': 1992 
    },
    'LSTM': {
        'h': 28,
        'input_size': -1,
        'max_steps': 10,
        'stat_exog_list': ['item_id', 'dept_id', 'cat_id', 'store_id', 'state_id'],
        'futr_exog_list': ['year', 'quarter', 'month', 'week', 'dayofweek', 'day', is_weekend, 'event'],
        'random_seed': 1992 
    },
    'TCN': {
        'h': 28,
        'input_size': -1,
        'max_steps': 10,
        'stat_exog_list': ['item_id', 'dept_id', 'cat_id', 'store_id', 'state_id'],
        'futr_exog_list': ['year', 'quarter', 'month', 'week', 'dayofweek', 'day', is_weekend, 'event'],
        'random_seed': 1992 
    },
    'NBEATSx': {
        'h': 28,
        'input_size': 7,
        'max_steps': 10,
        'stat_exog_list': ['item_id', 'dept_id', 'cat_id', 'store_id', 'state_id'],
        'futr_exog_list': ['year', 'quarter', 'month', 'week', 'dayofweek', 'day', is_weekend, 'event'],
        'random_seed': 1992 
    },
    'NHITS': {
        'h': 28,
        'input_size': 7,
        'max_steps': 10,
        'stat_exog_list': ['item_id', 'dept_id', 'cat_id', 'store_id', 'state_id'],
        'futr_exog_list': ['year', 'quarter', 'month', 'week', 'dayofweek', 'day', is_weekend, 'event'],
        'random_seed': 1992 
    }    
}
target_transforms: null
features: {
    'static': ['item_id', 'dept_id', 'cat_id', 'store_id', 'state_id'],
    'lags': null,
    'lag_transforms': null,
    'date': ['year', 'quarter', 'month', 'week', 'dayofweek', 'day', is_weekend],
    'xregs': ['event']
}