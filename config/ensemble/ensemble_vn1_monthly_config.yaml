
dataset: {
    'dataset_name': 'vn1',
    'frequency': 'monthly',
    'ext': '.parquet'
}

fitting: {
    'horizon':6,
    'test_window': 18,
    'retrain_scenarios': [
        1, 
        2, 
        3,
        4, 
        5,
        6,
        9,
        12,
        15,
        18
    ]
}

ensembling: {
    model_names: [
        ['NHITS', 'NBEATSx', 'MLP'],
        ['LinearRegression', 'XGBRegressor', 'CatBoostRegressor']
    ],
    methods: ['mean'], # ['mean', 'median']
    name: ['TOP3ACC', 'TOP3TIME']
}
