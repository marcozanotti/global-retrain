TODO:

1. impostare l'analisi finale
- funzione obiettivo con costi per scelta di retrain_window (max cost 20k)


NOTE:

- valutare l'aggiunta di datasets (Rossmann, Favorita, M4, Wikipedia)
- valutare l'aggiunta di un parametro skip per saltare di k le iterazioni 
(deve essere minore di horizon e della retrain_window)
(se uguale alla retrain window produce previsioni non overlapping)
- valutare il padding del dataset per averle tutte lunghe uguali (necessario per insample DL)
- valutare il save e load dei modelli ad ogni fitting
- aggiungere encoding per categorical variables
https://nixtlaverse.nixtla.io/mlforecast/docs/how-to-guides/sklearn_pipelines.html

Seasonality values corresponding with the frequencies: minutely, 10_minutes, 
half_hourly, hourly, daily, weekly, monthly, quarterly and yearly
Consider multiple seasonalities for frequencies less than daily
# SEASONALITY_MAP = {
#    "minutely": [1440, 10080, 525960],
#    "10_minutes": [144, 1008, 52596],
#    "half_hourly": [48, 336, 17532],
#    "hourly": [24, 168, 8766],
#    "daily": 7,
#    "weekly": 365.25/7,
#    "monthly": 12,
#    "quarterly": 4,
#    "yearly": 1
# }

# find the best model
# con tutti i valori dei modelli sulle colonne
# es. unique_id, LinearRegression, Lasso, etc
# evals['best_model'] = evals.idxmin(axis=1)