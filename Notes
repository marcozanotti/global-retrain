# TODO: HapagLloyd Project
- check manually results on Naive, WindowAverage, ETS
- add lags and rolling, also rolling std 
- ML one-hot date variables? DL better with fourier at 52 and 13 or with scaled date variables? Do not double scale in DL
- tuning before on:
-- models: AutoXGBoost, AutoLGBM, AutoMLP, AutoNBEATSx
-- training:
-- validation: 104
-- test: 104

- next steps?
-- tuning pipeline


# NOTE:
- valutare l'aggiunta di datasets (Rossmann, Favorita, M4, Wikipedia)
- valutare l'aggiunta di un parametro skip per saltare di k le iterazioni 
(deve essere minore di horizon e della retrain_window)
(se uguale alla retrain window produce previsioni non overlapping)
- valutare il padding del dataset per averle tutte lunghe uguali (necessario per insample DL)
- valutare il save e load dei modelli ad ogni fitting
- aggiungere encoding per categorical variables
https://nixtlaverse.nixtla.io/mlforecast/docs/how-to-guides/sklearn_pipelines.html